#Implimented nginx reverse proxy to route traffic to backend and frontend containers.no longer need to set the VITE_API_URL in the frontend/.env.production file. 
#The frontend will now use relative paths to access the backend API through the nginx reverse proxy.No longer need to set the VITE_API_URL in the frontend/.env.production file. 
#The frontend will now use relative paths to access the backend API through the nginx reverse proxy.
# VITE_API_URL=http://32.237.31.64:8080

